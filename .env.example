# Backend API base URL
API_URL=http://localhost:8000

# Secret used to sign the user JWT
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Show the ticket number instead of the order code (true/false)
SHOW_NUMBERS=false
